path=(~/.plenv/bin(N-/) $path)
typeset -xT PERL5LIB perl_path
typeset -U perl_path
perl_path=(./lib(N-/))

if [ -e ~/.plenv ];  then
  eval "$(plenv init - zsh)"
  if [ -e ~/.plenv/completions/plenv.zsh ]; then
    source ~/.plenv/completions/plenv.zsh
  fi
fi

